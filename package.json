{
  "name": "ianjamesormo-website",
  "version": "1.0.0",
  "description": "Ian James Ormo — author site. Static HTML + React via CDN.",
  "private": true,
  "scripts": {
    "start": "serve -l tcp://0.0.0.0:${PORT:-3000} -s .",
    "dev": "serve -l 3000 ."
  },
  "dependencies": {
    "serve": "14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
